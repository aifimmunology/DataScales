# RAPIDS User Notebook

Standard-usage notebook(s) for GPU single-cell analysis with `rapids-singlecell` — the
reference path for moving AnnData to device, running the pipeline, and accounting for
host↔device transfer. GPU/CUDA-only (runs on an HPC node, not the local CPU env).

Notebooks and supporting scripts live here.
