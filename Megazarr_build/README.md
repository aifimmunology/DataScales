# MegaZarr Build

Building the **MegaZarr** — one large Zarr store assembled from multiple single-cell cohorts —
plus the rationale and layout choices (chunking, sparse/dense, sharding) for a store at that scale.

Build scripts live here.
